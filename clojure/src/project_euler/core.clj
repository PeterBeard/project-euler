(ns project-euler.core
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "To run a particular solution use \"lein with-profile problem-xxx run\"")
  (println "For example, to see the solution to problem 1, you would type this:")
  (println "    lein with-profile problem-001 run"))
